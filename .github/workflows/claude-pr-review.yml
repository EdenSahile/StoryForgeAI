name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write
  id-token: write

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npx vitest run

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test

  claude-review:
    name: Claude review
    runs-on: ubuntu-latest
    needs: [test, e2e]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          show_full_output: true
          claude_args: |
            --allowedTools "Bash(gh pr view:*),Bash(gh pr diff:*),Bash(gh pr review:*)"
          prompt: |
            You are reviewing PR #${{ github.event.pull_request.number }}
            specifically. Use `gh pr view ${{ github.event.pull_request.number }}`
            and `gh pr diff ${{ github.event.pull_request.number }}` directly —
            do not run `gh pr list` to discover it.

            Review this pull request against the conventions in CLAUDE.md
            (never expose the Anthropic API key client-side, never return raw
            error.message to the client per SEC-001, brief validated server-side
            at 2000 chars, 30s timeout on Claude calls, max_tokens capped at 1000,
            French system prompt/response). Check for correctness bugs, security
            issues, and consistency with existing patterns in the codebase.

            The `test` and `e2e` jobs already ran `npx vitest run` and
            `npx playwright test` in CI before this step — treat that as
            ground truth for whether the suites pass. Don't re-run them
            yourself; run a single focused test only if you need to double
            check a specific concern.

            Do not run `gh pr checks`, `gh api`, `gh pr list`, or any other gh
            command beyond `gh pr view`, `gh pr diff`, and `gh pr review` — you
            do not have permission for anything else, and the `test`/`e2e`
            jobs' status is already known to have passed by the time you run
            (this job depends on both via `needs: [test, e2e]`). Go straight
            to reviewing the diff.

            Categorize findings as Critical / Important / Minor, same as this
            project's /code-review skill would.

            When you're done:
            - If there are no Critical or Important findings, submit an
              approving review: `gh pr review --approve --body "<short summary>"`.
            - If there are Critical or Important findings, request changes:
              `gh pr review --request-changes --body "<findings with file:line>"`.

            Do not merge the PR yourself and do not push commits — your only
            job is to leave the review verdict. Merging is handled separately
            by branch protection + auto-merge once required checks pass.
